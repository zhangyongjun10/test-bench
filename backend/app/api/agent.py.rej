--- app/api/agent.py
+++ app/api/agent.py
@@ -40,6 +40,8 @@ async def list_agents(
     service = AgentService(session)
     agents = await service.list_agents(keyword)
     return Response[List[AgentResponse]](
-        data=[AgentResponse.model_validate(a) for a in agents]
+        data=[debug_validate(a) for a in agents]
     )
 
+def debug_validate(agent):
+    res = AgentResponse.model_validate(agent)
